#include <stdio.h>

int main() {
    int pages[10], frame[3], time[3], i, j, k = 0, faults = 0;
    for (i = 0; i < 3; i++) frame[i] = -1;

    for (i = 0; i < 5; i++) {
        scanf("%d", &pages[i]);
        for (j = 0; j < 3; j++)
            if (frame[j] == pages[i]) break;

        if (j == 3) {
            frame[k] = pages[i];
            k = (k + 1) % 3;
            faults++;
        }
    }
    printf("Faults: %d\n", faults);
    return 0;
}
