#include <stdio.h>

int main() {
    int index[10], n, i;
    scanf("%d", &n);
    for (i = 0; i < n; i++) scanf("%d", &index[i]);
    for (i = 0; i < n; i++)
        printf("Index %d -> Block %d\n", i, index[i]);
    return 0;
}
