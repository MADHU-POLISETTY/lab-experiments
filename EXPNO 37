#include <stdio.h>

int main() {
    int q[5], i, head, seek = 0;
    for (i = 0; i < 5; i++) scanf("%d", &q[i]);
    scanf("%d", &head);

    for (i = 0; i < 5; i++) {
        seek += abs(q[i] - head);
        head = q[i];
    }
    printf("Seek Time: %d\n", seek);
    return 0;
}
