#include <stdio.h>

int main() {
    int pages[20], frame[3] = {-1,-1,-1}, i, j = 0, faults = 0;
    int n = 5;
    for (i = 0; i < n; i++) scanf("%d", &pages[i]);

    for (i = 0; i < n; i++) {
        frame[j] = pages[i];
        j = (j + 1) % 3;
        faults++;
    }
    printf("Page Faults: %d\n", faults);
    return 0;
}
